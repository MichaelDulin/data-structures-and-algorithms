'use strict';

const sortByTitle = (arr) => {
  return arr;
};

const sortByYear = (arr) => {
  return arr;
};

const compare = (a, b) => {
  return a, b;
};

compare(1,2);
sortByYear([1]);
sortByTitle([1]);
